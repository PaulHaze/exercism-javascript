/* 
describe('Transform', () => {
  test('transforms one value', () => {
    const old = { 1: ['A'] };
    const expected = { a: 1 };

    expect(transform(old)).toEqual(expected);
  });
*/

const transform = obj => {
  return obj;
};
